@extends('layout')
@section('title',$type==='invite'?'Einladung annehmen':'Passwort zurücksetzen')
@section('content')<section class="panel auth-card"><h1>@yield('title')</h1><form method="post" class="form-stack" action="/zugang/{{ $type }}/{{ $token }}">@csrf @if($type==='invite')<label>Dein Name<input name="name" required maxlength="100" autocomplete="name" value="{{ old('name') }}"></label>@endif<label>Neues Passwort (mindestens zwölf Zeichen)<input type="password" name="password" required minlength="12" autocomplete="new-password"></label><label>Passwort wiederholen<input type="password" name="password_confirmation" required minlength="12" autocomplete="new-password"></label><button class="primary">{{ $type==='invite'?'Mitglied werden':'Passwort speichern' }}</button></form></section>@endsection
